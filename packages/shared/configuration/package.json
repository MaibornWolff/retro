{
  "name": "@shared/configuration",
  "version": "1.0.0",
  "description": "Provides configurations for the Retro application",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "rimraf dist && tsc -b",
    "build:watch": "rimraf dist && tsc -b --watch"
  }
}
