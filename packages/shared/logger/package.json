{
  "name": "@shared/logger",
  "version": "1.0.0",
  "description": "Provides logger",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "rimraff dist && tsc -b",
    "build:watch": "rimraf dist && tsc -b --watch"
  },
  "dependencies": {
    "winston": "^3.8.2"
  }
}
