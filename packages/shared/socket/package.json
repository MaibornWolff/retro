{
  "name": "@shared/socket",
  "version": "1.0.0",
  "description": "Provides reusable events for socket.io",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "rm -rf dist && tsc -b",
    "build:watch": "rm -rf dist && tsc -b --watch"
  }
}
