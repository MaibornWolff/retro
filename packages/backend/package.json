{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon --esm src/index.ts",
    "start-peer-server": "peerjs --port 3002",
    "build": "rimraf dist && tsc -b"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "peer": "^0.6.1",
    "socket.io": "^4.5.4"
  },
  "devDependencies": {
    "@shared/socket": "*",
    "@shared/logger": "*",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1"
  }
}
